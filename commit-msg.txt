fix: lift dsh composer/inputs above the maid stage so the input row isn't painted over

The non-chat maid stage sits at z=2 (above content z-auto, below dialogs
z>=50). At 96vh the bottom half of the side characters extends across
the bottom of the viewport and covers the dsh composer / input row.

Promote composerSeat + composerCard + inputMirror + MessageRow card to
z-index 100 within body[data-dsh-maid-atelier]. The stage stays z=2
(visible as background art), but anything the user can actually click
on (composer, message bubbles) sits above it. Scoped to maid so other
skins are untouched.

Verified in playwright: maid stage z=2, composerSeat + composerCard
both z=100.