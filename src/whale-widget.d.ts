/**
 * Vendored DSH balance whale widget (MeteorNOX/DeepSeek-Balance-Whale-Widget,
 * MIT). Bundled into dsh-beautify so the "pet + balance" companion ships with
 * the skin plugin. The implementation is compiled JS kept verbatim; this
 * declaration mirrors its exports for TypeScript consumers.
 */
export const name: string
export const inject: readonly string[]
export function apply(ctx: unknown): void
